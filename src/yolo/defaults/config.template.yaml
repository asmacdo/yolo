---
# yolo configuration
#
# Config is loaded from these locations (later overrides earlier):
#   0. Package defaults
#   1. /etc/yolo/config.yaml        — system/org
#   2. ~/.config/yolo/config.yaml   — user preferences
#   3. .yolo/config.yaml            — project (committed to git)
#   4. .git/yolo/config.yaml        — project (local, untracked)
#
# Merge rules:
#   - Lists: append across layers
#   - Dicts: recurse and merge
#   - Scalars: later value wins
#   - Use !replace to override a list instead of appending:
#       volumes: !replace
#         - ~/only-this

# ── Env vars ────────────────────────────────────────────────
# Passed into the container.
# Bare name = passthrough from host. KEY=VALUE = set explicitly.
# env:
#   - MY_CUSTOM_VAR
#   - MY_SECRET=hunter2

# ── Volumes ─────────────────────────────────────────────────
# Extra bind mounts. Shorthand:
#   ~/data           → ~/data:~/data            (1-to-1)
#   ~/data::ro       → ~/data:~/data:ro        (1-to-1, read-only)
#   /host:/container → /host:/container         (partial)
#   /h:/c:opts       → /h:/c:opts              (full, unchanged)
# volumes:
#   - ~/data
#   - ~/projects::ro

# ── Launcher ────────────────────────────────────────────────
# Git worktree handling: ask (default), bind, skip, error
# worktree: ask

# NVIDIA GPU passthrough via CDI
# nvidia: false

# ── Image ───────────────────────────────────────────────────
# Which image to run. Must be defined in an images.yaml file.
# Default: yolo-default (ships with yo).
# image: yolo-default
#
# Image definitions live in images.yaml (separate from config.yaml).
# See images.yaml locations:
#   <package>/defaults/images.yaml   — builtins
#   /etc/yolo/images.yaml            — system/org
#   ~/.config/yolo/images.yaml       — user
#   .yolo/images.yaml                — project (committed)
#   .git/yolo/images.yaml            — project (local)
